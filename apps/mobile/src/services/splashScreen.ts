export async function preventAutoHideSplashScreen() {
  // Splash screens auto-hide by default in Expo Go, so this is a no-op placeholder.
}

export async function hideSplashScreen() {
  // No-op placeholder for environments without expo-splash-screen installed.
}
